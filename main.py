from src.Bin_Class_NLP_code import binary_classification

# This script serves as the entry point to execute the binary classification function.
# It imports the function from another module and runs it when the script is executed directly.

if __name__ == "__main__":
    # Calls the binary_classification function to train and evaluate the model
    binary_classification()
